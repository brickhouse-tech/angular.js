name: auto-release

on:
  push:
    tags:
      - 'v*'

jobs:
  # Wait for CI to pass on this ref
  ci:
    uses: ./.github/workflows/ci.yml

  release:
    needs: [ci]
    uses: brickhouse-tech/.github/.github/workflows/auto-release.yml@main
