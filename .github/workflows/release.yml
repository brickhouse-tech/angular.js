name: release

on:
  push:
    branches: ["master"]
    paths-ignore:
      - '.devcontainer/**'
    tags-ignore: ['**']

jobs:
  release:
    uses: brickhouse-tech/.github/.github/workflows/release.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
